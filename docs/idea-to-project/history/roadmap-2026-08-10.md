# Roadmap — 蒸汽都市

Last updated: 2026-08-10 · Current baseline: V1.10 修订⑦

> **非规范历史**：本文件冻结 2026-08-10 前端审计及返工流水，其中“当前”“done”和测试数量仅代表当时快照。现行状态/延期/下一步只看 `../status.md` 与 `../backlog.md`。

## 当前基线

- 已上线：<https://web1800.top>
- 技术：原生 JS + Canvas + DOM；Cloudflare Pages；留言 Worker + D1
- 数据：64 建筑、63 商品、5 阶层
- 开局：随机 128×128 空岛；10000 金币/60 木/300 鱼
- 可玩阶层：农民、工人、工匠；工程师/投资人锁定
- 测试：52/52（V1.10 修订⑥）
- 浏览器资源：v96

## 已完成轮次：V1.10 修订⑦——文档基线同步

范围：B-32 / REQ-62 / AC-09~10。

### 目标

1. README、package、AGENTS 和当前规范与代码/测试一致
2. 30×30 固定地图、森林、旧收入、赠送仓库等旧规则只存在于明确标注的历史文件
3. B-07/B-09/随机地图等已完成功能不再显示 pending
4. 玩家机制速览同步中心锚点与住宅满员升级条件
5. 自动化测试、浏览器 smoke 和文档漂移审计全部通过

### 验收结果

- `decision-history.md` 和 `roadmap-history.md` 保存旧流水，并明确标注非规范历史
- README、package、AGENTS、Product Brief、Requirements、Architecture、Decisions、Backlog、Roadmap、经济说明和玩家指南已统一
- 文档语义审计：12 份当前文档，0 项漂移；package=`web1800`；README/浏览器标题仅显示“蒸汽都市”
- 语法检查：`src/main.js`、`population.js`、`guide.js`、`gen-needs-js.py` 全部 supported
- 自动化：`npm test` 52/52 supported
- 部署隔离：`deploy/` 36 个运行文件，不含 README、docs、tools 或 `.git`
- 浏览器：28 个资源全部加载 v96；机制速览显示“该栋住宅满员”和“几何中心”，旧锚点文案不存在；控制台 0 错误
- 本地完成，尚未自动提交、推送或部署

## 当前轮次：V1.10 修订⑧——首屏与 HUD 适配

范围：B-33~35 / REQ-24 / AC-11~13。

1. **初始相机**：页面装配时定位到地图中心，首屏展示岛屿核心区
2. **顶部栏**：约 1258×622 紧凑桌面视口中保持四区有效宽度、按钮不重叠
3. **欢迎层**：正文独立滚动，开始按钮固定在可见操作区

不包含移动端触控、PWA、侧栏重构或美术升级。

验收结果（supported）：

- 1258×622：相机从 `{x:0,y:0}` 改为 `{x:1499,y:1593}`，视口中心命中可建平地 `(66,59)`
- 滚轮缩放后焦点格保持 `(66,59)`；右键拖拽改变相机，重新聚焦可恢复起始位置
- 1258×622：四个顶部 section 宽度均大于 0，11 个可见按钮全部在视口内且无矩形重叠
- 1258×480：欢迎正文独立滚动，开始按钮底部 455px，小于 480px 视口并保持完全可见
- 浏览器 28 个静态资源全部为 v98，控制台 0 JavaScript 错误
- `npm test` 52/52；部署白名单 36 个文件且不含 README/docs/tools/.git

## 中期路线

### 可玩性与平衡

- B-36：完整跑通农民→工人→工匠真实流程，记录供需、维护费、开发度和物流覆盖瓶颈
- 补充“长时间停留开局”“断粮后恢复”“多仓库扩张”等模拟场景
- 平衡数值只在用户明确要求后写入人工核查表并重新生成

### 内容闭环

- 补蜡烛及当前缺失的工匠链
- 补皮草大衣、大学、剧院、朗姆酒、咖啡、电力、银行等需求来源
- 完成高阶需求闭环后再讨论工程师/投资人解锁，不先降低 `999999`

### 扩展设计（未排期）

- 贸易、船运、多岛和新区域
- 仓库容量与运输车辆时间模型
- 移动端触控/PWA
- 音效、视觉强化和性能进一步优化

## 发布现状

- Cloudflare Pages：`bash tools/deploy.sh` → `deploy/`
- Worker 路由：`web1800.top/api/*`
- 留言字段：`id,nick,text,ts,is_dev,parent_id,status`
- 管理页：`/admin.html`，管理员 key 只在页面内存中使用
- 当前本地修复尚未自动提交、推送或部署

## 玩家反馈闭环

| # | 状态 | 结果 |
|---:|---|---|
| 1 | done | 旧缓存导致点击无响应 → 页面提示刷新并持续递增静态资源版本 |
| 2 | done | 开局金币 2500→10000 |
| 3 | done | 资源速率 `/t`→`/min` |
| 4/4b | done | 住宅显示单栋住户；升级要求该栋满员 |
| 5 | done | 需求消耗从误用每住宅值改为除容量后的每人口值 |
| 6 | done | 多仓库改为共享总资源池接入点 |
| 7 | done | 锚点格写入 grid，消除连通/重叠盲区 |
| 8 | done | 锚点改为建筑几何中心，旋转全链路统一 |
| 9 | done | 开发度以平地为可开发格，水/山/矿/黏土计入占用 |

## 交接实施：H-01~H-07 + M-01~M-06（2026-08-10，Sol 审计交接方案）

| 项 | 状态 | 结果 |
|---:|---|---|
| H-01 | done | /min 改为 60 tick 滚动平均（smoothMin），周期建筑不再 0/60 跳变，暂停保持稳定值；引擎测试 |
| H-02 | done | 资源栏重构：⭐关注（金币/木材/鱼 + 自动加入负增长/即将耗尽）+ 建材/需求展开、原料/其他折叠 |
| H-03 | done | computeStatus 返回 {status, reason, detail}（road-disconnected/workforce-shortage/input-shortage/warehouse-out-of-range/development-too-low），UI 中文主因+数值+建议；引擎测试 |
| H-04 | done | 等待/断连统计可点击展开异常列表，条目/上一下一栋定位（相机居中+高亮+详情）；小地图 z-index 11 不再被右栏遮挡 |
| H-05 | done | selectedBuildingId + redraw 每 tick 刷新详情；拆除后显示"已拆除" |
| H-06 | done | 模式提示条（正在放置:名称｜成本｜R 旋转｜Esc 取消）；Esc 统一取消；移动模式 R 真实旋转；删除"右键取消"错误文案 |
| H-07 | done | @media (max-width:1100px) 侧栏收窄至 196px、bottom 152px 避开底栏；@media (max-height:520px) 低高度同样避开 |
| M-01 | done | 人口趋势箭头 ↑↓→；需求缺口百分比标注；负增长资源显示预计耗尽秒数 |
| M-02 | done | 升级条件逐项 ✅/❌（满员/基础需求/建材），未满足禁用按钮 |
| M-03 | done | 成本图标化（💰500 🪵10）；禁用显示具体缺口（💰缺100） |
| M-04 | done | 金币行收支分解：收入 X/min 维护 Y/min；复审修复：收入/维护**各自 60 tick 平滑**（smoothProducedMin/smoothConsumedMin），消除"平滑净+即时维护"窗口不一致（零人口不再虚增收入），新增引擎测试 |
| M-05 | done | 目标附操作按钮；复审修复：g3/g4 按 progress **动态指向缺口建筑**（已有土豆田→引导蒸馏厂；已有绵羊牧场→引导纺织厂） |
| M-06 | done | 通知分级（红/黄/绿/灰）；复审修复：引擎日志结构化原因（缺料带商品名），UI **按模板聚合**（「原木厂×2、纺织厂:缺少原料(羊毛)×3」），不再精确字符串聚合 |

## Sol 复审修复记录（2026-08-10，逐条）

| 项 | 状态 | 结果 |
|---:|---|---|
| 复审 1 | done | 阻断：computeStatus 对象化后 population.js 4 处仍按字符串比较（服务覆盖/住宅容量/住宅计数）→ 断路建筑被误计，全部改 `.status`；新增 H-03 回归测试（断路住宅不计容量/数量） |
| 复审 2 | done | H-06：selectBuilding 改走 setMode（模式提示条+按钮高亮同步）；建造菜单按钮 active 高亮（activeType）；右键单击取消（拖拽平移保留）；拆除提示改「金币不返还，其余 100% 返还」 |
| 复审 3 | done | H-04：异常列表不再闭包捕获陈旧 items——模块级 issueFilter/issueIdx，每 tick 用最新状态重算已展开列表（恢复/原因变化/拆除即时反映） |
| 复审 4 | done | H-02：需求组按**已解锁阶层**过滤（渐进披露）；默认建材展开其余折叠（内容 452px ≤ 一屏 472px）；折叠状态跨 tick 保留（prevOpen 以 summary 文本为键） |
| 复审 5 | done | M-01：人口 /min 趋势（引擎 rates.__pop 60 tick 平滑，首 tick 防污染；UI ↑↓ 方向 + 数值） |
| 复审 6 | done | H-03 其余缺口：详情面板产出/劳动力中文化（log→原木、farmers→农民）；测试补 warehouse-out-of-range 断言（五种原因全部有引擎测试） |
| 二轮 P0 | done | 旧存档 ratesHistory 形状迁移：v102 旧 `{arr,sum}` → 新 `{p,c,n,sp,sc,sn}`。双保险——save.js deserialize 迁移（重建空窗口）+ tick.js 形状防御（畸形窗口安全重建）；新增迁移测试（旧形状→load→2 tick 无异常→口径正常），第 58 用例 |
| 二轮 M-05 | done | 目标链缺失判定从"数量推断"改为**具体缺失列表**：goals.js 新增 chainMissing（链内每栋检查存在且连通），g3/g4 返回 `missing:[type...]`；UI 用 `missing[0]` 引导（逆序建造如只有蒸馏厂 → 正确指向土豆田）；新增逆序/正序双向测试，第 59 用例 |
| 二轮 H-06 | done | 模式提示条成本图标分资源映射（coin/wood/brick/steel/windows/concrete 各自图标），不再全显示木材；建造菜单 M-03 起已有同款映射，本次补齐 main.js 提示条 |
| 二轮 H-06b | done | 移动成功/建筑不存在分支统一走 `setMode('inspect')`（原硬编码 `mode='view'` 致内部状态非标准、按钮全不亮、提示残留）；DOM 回归实测：移动成功 → mode=inspect、查看按钮 active、移动提示清空 |
| 二轮 M-01 | done | 人口箭头与 /min 数值统一用引擎 `smoothMin` 口径（>0.05 ↑ / <-0.05 ↓ / 否则 →），删除 UI 侧 render 间差值比较（额外 redraw 不再把增长误置为 →）；删除模块级 prevPop 状态 |
| 二轮 M-05b | done | 链段状态三态区分（无实例/有实例全断连/任意一栋连通）：`chainStatus` 返回 have+connected+buildingId，多栋同型任意一栋连通即完成（修复 find 只看第一栋）；目标携带 `locateIds`（断连实例 id）→ UI 显示「📍 定位:XX(已建,待连接)」+ `locateId` action 直定位，不再诱导重复建造；新增两条测试（第一栋断连第二栋连通→完成；仅断连实例→定位），第 60 用例 |
| 二轮 HIGH | done | ratesHistory 部分损坏不再静默接受：统一 `normalizeRatesHistory`（save.js 导出，load 与 tick 运行时共用）——顶层非普通对象→空对象；p/c/n 须有限数值数组且三轨等长≤60；不信任保存的 sp/sc/sn 按数组重算；`__pop` 单独验证 {n,sn}；无字段时保持无字段（往返一致）。测试改用 `Number.isFinite` 断言 + **真正 localStorage+load() 链路**（注入 mock 并在测试内恢复，防并发污染），覆盖缺失累计值/三轨不等长/畸形 __pop/非对象顶层四复现，第 61 用例；连续 3 次全绿 |
| 阻断二 | done | M-05 定位按钮名实错位：`missing[]`+`locateIds[]` 两数组无对应关系（土豆田未建+蒸馏厂断连时按钮写"定位土豆田"实际跳蒸馏厂）→ 改**结构化 `locateTargets:[{id,type}]`**，UI 从同一对象读 id 与显示名；新增回归测试（烈酒链+工作服链各一：未建链首+断连链尾 → locateTargets 指向链尾且按钮名=跳转建筑），第 62/63 用例；浏览器实测按钮「📍 定位:蒸馏厂」action.id=dist-1 ✓ |
| 唯一阻断 | done | 有限元素求和仍可溢出（60×1e308 → Infinity）：`normalizeRatesHistory` 重算累计值后**再验证 sp/sc/sn（含 __pop.sn）有限**，任一轨道溢出则丢弃整窗口（后续 tick 建空窗口）；新增真实 load 回归（溢出存档 → load → 2 tick 全口径 `Number.isFinite`，页面无 Infinity），第 64 用例；连续 3 次全绿 + 浏览器注入实测 coin/收入/维护/__pop 全部有限 |

后续 Sol 复审修复继续追加到本表；每次代码改动同步补记（文档纪律，2026-08-10 用户重申）。

后续玩家反馈继续追加到本表，不静默混入当前迭代范围。
