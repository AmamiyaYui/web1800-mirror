# AGENTS.md — web1800 开发指引

## 项目定位

《蒸汽都市》（英文项目名 `web1800`）是零依赖浏览器工业时代模拟经营游戏。当前不是脚手架阶段：随机地图、生产链、人口需求、住宅升级、仓库物流、道路等级、存档、留言和部署均已落地。机制正确性优先于新增内容。

## 续接顺序（必须）

1. `docs/idea-to-project/status.md` — 当前版本、本轮范围、下一决策
2. `docs/idea-to-project/backlog.md` — 唯一范围来源
3. `docs/idea-to-project/requirements.md` — 当前行为与验收
4. 按需读取 `architecture.md` / `decisions.md` / `references/`

`docs/idea-to-project/history/` 只供追溯，**不得用于推断现行规则**。

开始修改前必须核对代码、测试和浏览器接线，不能只相信历史文档。

## 硬性约定

1. **引擎 DOM-free**：`src/engine/` 不引入 `document`、`window`；`save.js` 只能用能力检测访问 localStorage。
2. **双端导出**：引擎模块同时挂载 `root.Engine.<module>` 和 `module.exports`；新增模块要同步登记 `index.html` 与测试加载顺序。
3. **运行时依赖后取值**：跨模块引用若加载顺序不保证，必须在函数体内读取，禁止顶部解构出未初始化模块。
4. **数据驱动**：建筑/需求数值来自人工核查表生成文件；未经用户明确指令，不自动同步所谓“原版数值”，也不直接手改 generated JS。
5. **中心锚点**：`building.x/y` 是建筑几何中心；旋转必须绕中心，所有 footprint、渲染、连通和移动逻辑保持一致。
6. **多仓库模型**：所有仓库都是同一 `state.resources` 总资源池的接入点；连任意仓库即连通，仓库彼此不要求互连，不建立孤立库存。
7. **海岸建筑**：footprint 必须全部是水格，同时至少一个 footprint 外侧四邻接陆地。
8. **矿场嵌合**：资源建筑 footprint 必须完全覆盖匹配矿脉；不可恢复为“只检查锚点”。
9. **需求口径**：原表“消耗/住宅(每秒)”和“收入/住宅(每分钟)”均除以阶层住宅容量，生成每人口单位；运行时按当前人口结算。
10. **测试顶层编排**：叶子函数测试不够；修改收入、tick、存档或输入编排时必须通过公共协调器写集成测试。
11. **移除机制要扫残留**：删除或替代机制后，搜索旧字段、提示文字、教程门禁和测试；禁止新旧规则并存。
12. **静态缓存**：任何浏览器资源变更后，`index.html` 中所有 `?v=N` 必须统一加一，并浏览器验证真实加载版本。
13. **需求轮闭环**：文档先行 → 实现/测试 → 浏览器 smoke → 回填 status/backlog；重要审计证据归档到 history；每轮最多 3~4 项。
14. **缺陷修复闭环**：规则明确时走“证据冻结 → 失败测试 → 根因修复 → 开发自验 → 停止修改 → 独立复验”；规则不明确或行为需要改变时，停止修复并切回需求迭代协议。
15. **审修分离**：Sol/审计方只定义问题、验收门和独立复验，不边审边改开发方源码；开发方按指定基线修复，交付新 HEAD 后停止修改。

## 每轮迭代协议

1. **选范围**：只从 backlog 选 3~4 项，绑定 REQ/AC；超范围项退回 backlog。
2. **文档先行**：先更新受影响的 requirements/architecture/decisions；被替代规则移入 history。
3. **实现与测试**：引擎变更先写失败测试；UI 和浏览器接线随后完成。
4. **验证**：执行 `npm test`、相关浏览器 smoke、`git diff --check` 和部署隔离检查。
5. **收尾**：更新 status 的基线/缺口/下一决策与 backlog 状态；需要保留的审计证据归档。

## 缺陷修复协议（必须）

固定闭环：

```text
发现问题
→ 判断规则是否明确
→ 冻结基线与复现证据
→ 开发方先写失败回归测试
→ 最小根因修复并扫描同类路径
→ 开发方完整自验
→ 停止修改并交付新 HEAD
→ Sol 独立针对性复验
→ 通过后同步文档与发布状态
```

### 1. 先分流：Bug 还是需求变更

- **规则已明确、实现违反现行 REQ/AC 或已确认行为**：按本协议修复，不重新讨论已经确定的规则。
- **规则缺失、规范与实现冲突且无法判定谁应保留、或期望行为发生变化**：立即停止修复，请用户拍板，然后切回“每轮迭代协议”，按文档先行处理。
- **新发现但不影响当前缺陷验收的问题**：登记 backlog，不顺手扩大本轮范围。
- **生产环境问题**：先判断安全、凭据、存档破坏和不可逆数据风险；需要止血时先决定回滚或热修，再进入同一闭环。不得把 push/自动构建当成生产验证。

### 2. 冻结问题证据

每个问题必须绑定当前完整 SHA，并记录：

```text
严重度(HIGH / MEDIUM / LOW)
归属(本轮引入 / 既有问题 / 规则待决)
文件:行号
最小复现
实际结果
预期结果
影响范围
禁止顺手修改的范围
```

- 审计结论只对绑定 SHA 有效；HEAD 已变化时，先把旧发现映射并复现在新 HEAD，过时快照不得推翻新实证。
- HIGH 直接阻断；MEDIUM 若影响 AC、状态一致性或必要回归证据也阻断；LOW 明确记入 backlog 后可不阻断。
- 功能当前“看起来正确”但必要测试没有执行生产公共路径，仍属于交付阻断，不能仅凭测试数量放行。

### 3. 开发方修复顺序

1. 在指定基线复现问题并定位根因，不能根据现象猜改。
2. 先增加能在旧代码上失败的回归测试，再修改生产代码。
3. 修根因而非表面症状；搜索同一规则的引擎、UI、输入编排、提示、存档和测试调用点，避免新旧口径并存。
4. 只修改根因和必要调用点；独立问题退回 backlog，禁止借 Bug 修复做重构或扩玩法。
5. 测试必须在最终断言时真实保持声明状态，使用精确 oracle；禁止公式自等式、恒真表达式、未使用前置值或可掩盖漏算的宽松 `>=`。
6. 修改收入、tick、存档、输入编排或 UI 派生口径时，除叶子函数外必须执行生产公共入口；UI 测试应调用真实 render/handler，而不是在测试里重算同一公式。

### 4. 开发方自验门

完成修复后至少执行：

```text
npm test
修改过的 JavaScript 语法检查
git diff --check
相关真实浏览器场景 smoke
版本化资源存在性、统一版本和 HTTP 加载检查
deploy/ 白名单隔离检查
敏感信息与异常附件检查
```

- commit/push 前必须人工执行并检查：`git status --short`、`git diff --check`、`git diff --cached --check`、`git diff --cached --name-status`、`git diff --cached`；扫描 staged 内容中的凭据、令牌、密码、Cookie、Authorization、私钥、连接字符串、真实存档、日志、截图和临时附件。
- 浏览器资源变化必须统一递增 `?v=N`；真实页面需确认加载新版本、无旧缓存混入、控制台 0 JavaScript 错误。
- 测试至少包含失败路径、成功路径和公共协调路径；修复数值守恒或状态迁移时必须同时断言操作前后总量与即时状态。
- 审计服务器结束时必须关闭，审计 localStorage 必须清理。

### 5. 开发方交付后停止修改

交付必须列出：

```text
修复基线完整 SHA
新的完整 HEAD
基线..新 HEAD 精确 diff
修改文件
新增/调整测试及真实测试数量
浏览器复现与修复后结果
资源缓存版本
git diff / 部署隔离 / 敏感信息结果
```

以下状态必须分开报告：

```text
本地 commit
push 到 origin/main
deploy/ 白名单包组装
自动构建是否触发
线上部署
生产站点验证
```

交付后开发方停止修改；Sol 复验期间不得并发补丁、改文档或换基线。

### 6. Sol 独立复验

- Sol 不直接修开发方源码；按上一轮剩余阻断逐项重跑最小复现、回归测试、公共路径、浏览器和工程门。
- 复验不仅看 `npm test` 是否全绿，还要确认测试确实驱动目标状态并执行了被修改的生产代码。
- 通过：批准新 HEAD，再由 Sol 更新现行 status/backlog/requirements/architecture/decisions 中受影响内容，并按需归档审计证据。
- 通过后追加**复盘**（仅 bug 修复/紧急错误需要；需求向迭代不做复盘）：根因链条是否被完整理解（如"错误可见化→截图破案"类流程是否前置）、同类缺陷是否扫描、需要沉淀到技能/文档的经验（如分层重构的作用域断裂风险、模拟环境与真实浏览器差异）；复盘结论写入 history/ 或现有类级技能 references/，不建一次性窄技能。
- 不通过：输出新的严重度、精确位置、复现、实际、预期、根因和最小返工门；开发方基于未批准 HEAD 继续小增量修复，禁止重写历史。

### 7. 文档更新边界

- **纯实现 Bug**：修复前不篡改正确的 requirements 来迁就代码；通过后只同步 status/backlog 和必要审计记录。
- **Bug 暴露稳定工程约束**：通过后把长期规则写入 AGENTS/architecture/decisions，避免只留在聊天或历史审计中。
- **玩法规则发生变化**：不再视为普通修复，切回需求协议，先改 requirements/AC，再实现和验收。
- `history/` 只保留追溯证据，不能充当现行规则来源。

## 当前事实速查

- 地图：随机 160×160 单主岛(World/Island schema；旧档 128×128 迁移居中+16)，森林已移除
- 开局：5000 金币、60 木材、100 鱼、0 人口、0 建筑([B-59])
- 内容：64 建筑、63 商品、5 阶层；工程师/投资人 `unlockAt=999999`
- 仓库：5×5、500 金币+10 木材、维护 20/min、土路范围 34
- 住宅：3×3，容量 10/20/30/40/50；只新建农民住宅
- 道路：土路 3/格；石板路从土路升级 12/格，传播成本 2/3
- 收入：`Σ(need.income × satisfaction × current population) / 60` 每 tick
- 测试：`npm test`；当前通过数量以 `status.md` 为唯一来源
- 发布：Cloudflare Pages，`bash tools/deploy.sh`，输出 `deploy/`

## 浏览器验证陷阱

- 页面会自动读取 localStorage；引擎探针使用 `Engine.state.createInitialState(seed)` 的独立状态。
- `clearSave()+reload` 会被 `beforeunload` 重写旧档；先替换 `Engine.save.save=function(){}` 再清档。
- 欢迎、图例、生产链等 overlay 会挡住底层按钮；先关闭 overlay 再点击。
- 审计存档会污染自动加载；每条存档 smoke 后必须清理 localStorage。
- 模式/道路等级会跨探针残留；每条 smoke 前重新选择工具并断言内部状态。

## 常用命令

```bash
npm test
python -m http.server 8000
bash tools/deploy.sh
```

`tools/release.sh` 会删除当前目录中的开发文件，只用于一次性克隆副本；本地和 Pages 默认使用非破坏性的 `deploy.sh`。

## 发布约定

- 仓库公开提交信息固定为 `Initial commit`。
- 不改历史、不 force-push。
- 发布前核对 `deploy/` 只含运行资产；原始表格、wiki 存档和内部对照资料不得进入静态站点。
